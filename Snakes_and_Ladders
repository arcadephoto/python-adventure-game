import random
#import game info

class snakes_and_ladders():
    MAX_VAL = 50
    #hard coded snakes
    snakes = {
        8: 4,
        18: 1,
        26: 10,
        39: 25,
        49: 16,
    }
    #hard code ladders
    ladders = {
        3: 20,
        6: 14,
        11: 28,
        15: 34,
        17: 74,
        22: 37,
        38: 47,
    }

#     def players(self):
#         self.player2 = "comp"
# no player two, challenge is to complete

    def welcome_msg():
        msg = """
        Welcome to Snakes and Ladders.


        Rules:
          1. Both players begin at starting position.
             Take turns rolling the dice.
             Move forward the number of spaces shown on the dice.
          2. If you lands at the bottom of a ladder, you can move up to the top of the ladder.
          3. If you lands on the head of a snake, you must slide down to the bottom of the snake.
          4. The first player to get to the FINAL position is the winner.

        """
        print(msg)

    def roll():
        min = 1
        max = 6
        dice_value = random.randint(min, max)

    def roll_the_dice(self):
        self.player_roll = input("press r to roll the dice or q to quit")
        if player_roll == "r":
            print("you rolled a {dice_value}")
            #self.player
        if ladders(n):
            print ("It\'s a ladder,Climb up")
            n = ladders[n]
        elif snakes(n):
            print ("It\'s a snake!go down")
            n = snakes[n]
        return dice_value

    def start():
        welcome_msg()

        player1_current_position = 0
        player2_current_position = 0

    while True:
        input_1 = input("\n" + self.player1 + ": " + random.choice(player_turn_text) + " Hit r to roll dice: ")#here
        print("\nRolling dice...")
        dice_value = roll_the_dice()
        print(self.player1 + " moving....") #here
        player1_current_position = old_

        input_2 = input("\n" + player2_name + ": " + random.choice(player_turn_text) + " Hit r to roll dice: ")
        print("\nRolling dice...")
        dice_value = get_dice_value()
        print(player2_name + " moving....")



    def snake_bite(old_value, current_value, player_name):
        print("\n" + random.choice(snake_bite).upper() + " ~~~~~~~~>")
        print("\n" + player_name + " got a snake bite. Down from " + str(self.health) + " to " + str()#####\ )
